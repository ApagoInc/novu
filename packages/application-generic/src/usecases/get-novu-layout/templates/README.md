At organization creation time,
the default "Novu" email layout is selected as the file that is named `layout.handlebars` from the dir `packages/application-generic/src/usecases/get-novu-layout/templates/` (starting at the root project directory.)
Any other layouts to be generated from files in that dir are selected by hardcoded names in the organization creation logic.
